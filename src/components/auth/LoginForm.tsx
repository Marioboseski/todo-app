const LoginForm = () => {
  return (
    <div>
      <form>
        <input type="text"
          placeholder="Enter name" />

        <input type="email"
          placeholder="Enter name" />

        <input type="password"
          placeholder="Enter name" />

        <button type="submit">Login</button>
      </form>
    </div>
  );
}

export default LoginForm;